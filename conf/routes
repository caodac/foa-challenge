# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
##
## for debugging purposes only!!
##
GET	/:id/@participant	controllers.ChallengeController.participant(id: String)
GET	/:id/@next	controllers.ChallengeController.next(id: String)
GET	/@list		controllers.ChallengeController.list

##
## main resources
##
GET	/start	controllers.ChallengeController.puzzle
GET     /    	controllers.ChallengeController.welcome
POST	/	controllers.ChallengeController.register
GET	/:id	controllers.ChallengeController.challenge(id: String)
GET	/:id/	controllers.ChallengeController.challenge(id: String)
GET	/:id/:stage	controllers.ChallengeController.stage(id: String, stage: Int)
#
# should really figure out how to properly handle CSRF filter
#
+ nocsrf
POST	/:id	controllers.ChallengeController.submit(id: String, stage: Int)

POST /c3/handler controllers.ChallengeController.handleC3Request()
POST /c2/handler controllers.ChallengeController.handleC2Request()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file  controllers.Assets.versioned(path="/public", file: Asset)
->	/webjars       webjars.Routes
